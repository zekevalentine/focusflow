# Addendum — Auth, Paywall, Checkout, and New UIs

This extends the MVP with:
- Magic link **authentication** (Sign in/out)
- **Paywall** (feature gating by plan)
- **Stripe Checkout** session creation
- **Account** page (plan status, manage subscription)
- **Cover Letter** UI (uses `/api/generate-cover-letter`)
- **Daily Focus** planner UI (uses `/api/daily-focus` and stores `daily_plans`)
- Basic **Header** with nav

Copy these files over the original starter bundle (keep your Next/Vercel configs).
